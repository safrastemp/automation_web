{
  "name": "automation-web",
  "version": "0.1.0",
  "description": "Automation workflow for an Angular web app to prevent idle logout and navigate key forms.",
  "scripts": {
    "start": "ts-node src/automation.ts",
    "build": "tsc"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "playwright": "^1.43.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
